# Finance Manager (Better Auth + Prisma) — Technical Test

Finance manager app with:
- Authentication/session via Better Auth
- Movements (income/expense) tracking
- Admin user management
- Reports (balance + chart) + CSV export
- Swagger/OpenAPI docs at `/api/docs`

## Tech Stack
- Next.js (Pages Router)
- TypeScript
- Prisma ORM
- PostgreSQL
- Better Auth (session/cookies)
- Vitest (unit tests)
- Recharts (reports chart)

---

## Requirements
- Node.js >= 18 (recommended)
- PostgreSQL database (local or hosted)
- npm (or your package manager if the repo uses another)

---

## Getting Started (Local)

### 1) Clone and install
```bash
git clone <YOUR_REPO_URL>
cd <YOUR_PROJECT>
npm install
